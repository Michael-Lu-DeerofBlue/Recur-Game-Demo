using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MarkerPos : MonoBehaviour
{
    // Start is called before the first frame update
    public Vector3Int SpanwPoint = new Vector3Int(0, 7, 0);
}
